#include "C_API.h"

void pushKey(char* scriptName){
    //stub
    /*
    lua_State* L
    pass script to lua state and run

    L = luaL_newstate();
    luaL_openlibs(L);

    if (luaL_loadfile(L, scriptName)){
        fprintf(stderr, "luaL_loadfile() failed");
    }    
    if (lua_pcall(L, _, _, _)){
        fprintf(stderr, "lua_pcall() failed")
    }
    return lua values from lua script
    store values somewhere

    values = lua_tonumber
    values = lua_tostring
    etc.

    Create a new luaState that will call pushKey
    lua_close(L);

    lua_State *L2:

    L2 = luaL_newstate();
    luaL_openlibs(L2);

    if (luaL_loadfile(L2, "libckrgb.lua"))
        error message

    l_getglobal(L2, "pushkey");
    if (lua_pcall(L, _, _, 0)){
        error message taht call failed
    }

    get return values and pass to libusb and then close
    close(L2)
    */
}

void pushKeys(char* scriptName){
    // lua_State* L
    // pass script to lua state and run

    // L = luaL_newstate();
    // luaL_openlibs(L);

    // if (luaL_loadfile(L, scriptName)){
    //     fprintf(stderr, "luaL_loadfile() failed");
    // }    
    // if (lua_pcall(L, _, _, _)){
    //     fprintf(stderr, "lua_pcall() failed")
    // }
    // return lua values from lua script
    // store values somewhere

    // values = lua_tonumber
    // values = lua_tostring
    // etc.

    // Create a new luaState that will call pushKey
    // lua_close(L);

    // lua_State *L2:

    // L2 = luaL_newstate();
    // luaL_openlibs(L2);

    // if (luaL_loadfile(L2, "libckrgb.lua"))
    //     error message

    // l_getglobal(L2, "pushkeys");
    // if (lua_pcall(L, _, _, 0)){
    //     error message taht call failed
    // }

    // get return values and pass to libusb and then close
    // close(L2)
    //stub
}

void pushAll;(char* scriptName){
    //stub
    // lua_State* L
    // pass script to lua state and run

    // L = luaL_newstate();
    // luaL_openlibs(L);

    // if (luaL_loadfile(L, scriptName)){
    //     fprintf(stderr, "luaL_loadfile() failed");
    // }    
    // if (lua_pcall(L, _, _, _)){
    //     fprintf(stderr, "lua_pcall() failed")
    // }
    // return lua values from lua script
    // store values somewhere

    // values = lua_tonumber
    // values = lua_tostring
    // etc.

    // Create a new luaState that will call pushKey
    // lua_close(L);

    // lua_State *L2:

    // L2 = luaL_newstate();
    // luaL_openlibs(L2);

    // if (luaL_loadfile(L2, "libckrgb.lua"))
    //     error message

    // l_getglobal(L2, "pushall");
    // if (lua_pcall(L, _, _, 0)){
    //     error message taht call failed
    // }

    // get return values and pass to libusb and then close
    // close(L2)
    return
}